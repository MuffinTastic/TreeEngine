struct TestDataOne
{
	f32[2] something;
	i32 somethingElse;
	u32 blah;
}

struct TestDataTwo
{
	f64[count] something;
}

// To be called from native code
managed system TestSystem
{
	bool PassData( i32 count, TestDataOne*[count] data );
}

// To be called from managed code
native system NativeLogger
{
	void LogBasic( enum level, string text );
}

system blah
{
	void LogBasic( enum level, string text );
}